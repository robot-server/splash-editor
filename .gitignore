build/
build-*/
.cache/
.DS_Store
compile_commands.json
CMakeUserPresets.json

# 테스트 맵은 저작권 자료이므로 커밋하지 않는다 (tests/maps/README.md 참고)
tests/maps/*.scm
tests/maps/*.scx
tests/maps/*.chk

# 파이썬 캐시
__pycache__/

# 학습에 쓴 **원본 맵은 저장소 어디에도 넣지 않는다.** 남의 저작물이다.
# 거기서 뽑은 파생 통계만 담는다
# (.agents/skills/starcraft-map/data/corpus.json — 숫자뿐, 맵 이름·지형 없음).
*.scm
*.scx
*.chk
*.mpq
*.trig.tmp
*.tiles
*.objects
