# StarCraft 맵 문서

**LLM 이 읽고 바로 쓰기 위한 문서다.** 사람이 읽는 안내서가 아니라,
맵을 만들거나 고칠 때 **짐작을 없애기 위한 사실 창고**다.

## 왜 만드나

이 저장소에서 맵을 만들면서 같은 실수를 되풀이했다. 뿌리는 하나다 —
**게임이 이미 아는 것을 손으로 표를 만들어 짐작했다.**

실제로 겪은 것들:

| 짐작한 것 | 게임이 이미 아는 것 |
| --- | --- |
| 램프 타일 표를 손으로 적음. 네 방향이 같은 세트였고 절반이 없는 타일이었다 | 타일셋 미니타일에 `Ramp` 표시가 있다 (`sc.h`) |
| "Space·Desert·Ice·Twilight 는 램프가 없다" | 넷 다 램프 타일이 있다 (Desert 607개) |
| 두뎃을 아무 지형에나 놓음 | 두뎃마다 `kind` 가 지형 종류 이름과 **같고**, `DoodadPlacibility` 가 놓을 수 있는 그룹을 정확히 말해 준다 |
| "유즈맵은 두뎃을 안 쓴다" (DD2 만 셈) | 두뎃은 저장할 때 지형으로 눌러 담긴다. 타일로 세면 92%가 쓴다 |
| 고도 문턱을 2 로 고정 | 타일셋마다 다르다 (Jungle 1, Badlands 2, Space 는 평지가 1) |

그래서 이 문서 묶음의 규칙은 하나다.

> **짐작해서 적지 않는다. 잰 것만 적는다.**
>
> 잰 값은 여기 적어 둔다 — 매번 다시 재는 것이 더 비효율이다. 대신
> **어떻게 쟀는지를 반드시 함께 적는다**: 어느 스크립트로, 몇 장에서,
> 어떤 자로. 자를 안 적으면 다음에 다른 자로 재고 다른 답을 얻는다
> (고도 기준을 세 번 다르게 잡아 세 번 다른 답을 얻은 적이 있다).
>
> 표가 길거나 기계가 읽어야 하는 것은 `data/*.json` 에 두고 여기서
> 가리킨다. 사람이 판단할 때 필요한 요약은 여기 적는다.

> **유닛 이름은 줄여 쓰지 않는다.** "듀란" 은 사미르 듀란(고스트)과
> 감염된 듀란 둘이고, "케리건" 도 사라 케리건(고스트)과 감염된
> 케리건(인페스티드 테란) 둘이다. 겹치는 이름이 아홉 갈래 있다
> ([unit/heroes.md](unit/heroes.md)). 줄여 쓰면 다음에 읽을 때 다른
> 유닛으로 읽힌다.

## 어떻게 읽나

제작 규칙을 적용할 때는 통계 색인보다 아래 기능별 패턴 문서를 먼저 본다.
기존 실측 문서와 JSON 산출물은 조사 근거와 재현성을 위해 보존한다. 빈도·평균·
중앙값은 해당 기능이 어떤 맵에서 어떤 조건과 배치로 쓰였는지 찾아갈 단서일 뿐,
새 맵의 기본값이나 품질 목표로 복사하지 않는다.

| 제작 과제 | 기능별 패턴 |
| --- | --- |
| 밀리맵 램프·멀티 자원 자리 | [melee/resource-sites.md](melee/resource-sites.md) |
| 유즈맵 상호작용·비콘·브리핑·생성 동선 | [usemap/functional-design.md](usemap/functional-design.md) |

**갈래는 지식 기준이다.** 어디서 알았는지(게임 데이터·실측 맵·카페)로
나누지 않는다 — 같은 주제를 세 군데에서 찾게 되기 때문이다. 출처는
문장마다 적는다.

| 갈래 | 무엇 | 글 |
| --- | --- | ---: |
| [tileset/](tileset/) | 지형 — 종류·고도·ISOM·램프·두뎃·색·스프라이트 | 8 |
| [chk/](chk/) | 맵 파일 구조와 실측 해부 | 3 |
| [unit/](unit/) | 유닛 — 고르는 것과 고치는 것. 영웅·공격 형태·예외·점수 | 7 |
| [trigger/](trigger/) | 트리거 — **인자 레퍼런스**·조건·액션·로케이션·AI·랜덤·스킬 | 14 |
| [game/](game/) | 게임이 실제로 어떻게 구는가 (시간·규칙·튕김) | 3 |
| [melee/](melee/) | 밀리맵 — 밸런스·지형·실패 기록 | 3 |
| [usemap/](usemap/) | 유즈맵 — 지형 문법·재미 구조·장르·소리 | 6 |
| [eud/](eud/) | EUD — 안전 규칙·원리·구조 오프셋·epScript | 5 |
| [tools/](tools/) | CLI·에디터·DatEdit·FireGraft | 4 |
| [method/](method/) | **재는 법과 실측 색인.** 어떤 자로 쟀는지, 어디서 틀렸는지 | 3 |

**모두 57편.**

## 어디까지 훑었나

| 출처 | 분량 | 상태 |
| --- | --- | --- |
| **게임 설치본** | 타일셋 8 · 유닛 228 · 무기 130 · 두뎃 2031 | `splash-cli` 로 직접 읽는다 |
| **scmscx.com 맵** | 밀리 260 · 유즈 421 (**90%가 래더·리그 맵**) | 지형 · 트리거 · 유닛 설정 · CHK 섹션까지 전수 |
| **강낭땅콩의 에디터 강좌** | 66편 | **전수** |
| **에디터의 모/든/것** | 83편 | 전수 훑고 새 사실 반영 |
| **Miscellaneous** | 33편 | 같음 |
| **인강으로 배우는 eps** | 19편 | **전수** |
| **맵 제작 120% 활용** | 23편 | 같음 |
| **EUD 간단 활용법** | 91편 | **글이 있는 17편만.** 74편은 이미지 강의라 못 읽는다 |
| 나무위키 · Liquipedia | 필요한 것만 | 출처 3급으로 씀 |

**적대적 교차 검증**을 세 번 받았다 (codex · grok · 검증 에이전트).
거기서 나온 정정은 [method/measuring.md](method/measuring.md) 의
출처 등급에 따라 반영했다.

## 출처

1. **게임 설치본** — 타일셋(CV5·VF4·VX4·WPE), 두뎃(dddata.bin),
   유닛 표. `splash-cli` 로 읽는다.
2. **scmscx.com** — 실제 맵. 밀리 260장, 유즈 421장을 내려받아 전수로
   떴다. **원본 맵은 이 저장소에 커밋하지 않는다.** 거기서 뽑은 통계만
   남긴다.
3. **스타 에디터 아카데미** (네이버 카페 `edac`) — 사람이 정리한 관용구와
   함정. 카페북 **여섯 권 315편**을 받아 훑었다. 본문뿐 아니라
   **댓글도 본다** — 본문을 정정하는 댓글이 있다 (튕기는 조건이
   "건설 불가" 가 아니라 "이동 불가" 라는 것을 댓글에서 찾았다).
4. **나무위키 · Liquipedia** — 게임 안에서 일어나는 일. 출처 3급.
5. **적대적 교차 검증** — codex · grok · 검증 에이전트. 재구현(OpenBW)을
   근거로 드는 것은 4급으로 다룬다 → [method/measuring.md](method/measuring.md).

## 재는 스크립트

모두 `.agents/skills/starcraft-map/scripts/` 에 있고, 결과는 같은 스킬의
`data/*.json` 에 쓴다.

| 스크립트 | 무엇을 재나 | 결과 |
| --- | --- | --- |
| `measure_terrain_types.py` | 지형 종류 → 타일 그룹·고도 | `terrain-types.json` |
| `measure_tile_colors.py` | 타일 그룹의 실제 색 (렌더러로) | `tile-colors.json` |
| `measure_doodads.py` | 두뎃이 걷기를 막는가 | `doodad-walk.json` |
| `measure_ramps.py` | 램프를 찾아 **걸어서** 검증 | `ramps.json` |
| `classify_genre.py` | 트리거 지문으로 장르 가르기 | `genre-fingerprints.json` |
| `corpus.py` | 실측 분포 읽기 | `corpus.json` |

## 여기에 무엇을 적지 않나

- **사용자의 맵 파일 이름.** 증상·수치·원인만 적는다.
- **원본 맵 파일.** 통계와 방법만 남긴다.
- **재면 되는 수치.** 스크립트와 결과 파일을 가리킨다.
