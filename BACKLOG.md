# 백로그

지금 당장 급하지 않지만 잊으면 안 되는 것들. 각 항목에 "무엇이 막고 있는지"를
적어 두었다 — 나중에 다시 붙을 때 조사부터 다시 하지 않기 위해서다.

## 그래픽

### 크립 가장자리가 게임과 다르다

건물마다 타원으로 범위를 잡고 테두리만 부드럽게 처리한다. 게임 화면과 나란히
놓으면 경계 모양이 다르다.

**막고 있는 것**: 게임의 크립 확산 패턴(건물별로 어느 타일까지 퍼지는지)이
데이터 파일이 아니라 실행 파일 안에 있다. 타일셋에 가장자리 전용 타일이 없다는
것은 확인했다 (README 의 "크립 가장자리에 대하여" 참고).

**다시 붙는다면**: 게임을 실제로 띄워 크립 모양을 관찰하고 타일 패턴을 표로
만드는 방법이 남아 있다. 실행 파일 역공학은 프로젝트 제약에서 금지.

### 유닛 애니메이션이 없다

정지 자세만 그린다. `AnimContext::animate(tick)` 에 타이머를 물리면 되지만,
에디터에서 유닛이 움직이면 좌표를 잡기 불편하다. SCMDraft 2 도 정지 화면이다.

### 리마스터(HD) 그래픽으로 그리지 않는다

지금은 클래식(SD) 자산만 쓴다 — 지형은 `tileset/*.cv5/.vr4/.wpe`(256색 팔레트),
유닛은 GRP, 아이콘은 `cmdicons.grp` 다. MappingCore 가 다루는 것이 이 경로라
그대로 따랐고, SCMDraft 2 도 SD 로 보여 준다.

리마스터 자산은 같은 설치본에 함께 들어 있다:

```
anim/main_000.anim       4.5 MB   (HD)
HD2/anim/main_000.anim   1.1 MB   (중간 해상도)
```

`.anim` 은 DDS(DXT 압축) 텍스처 아틀라스에 프레임 좌표·오프셋이 붙은 구조이고,
팀 색·그림자·발광을 따로 둔 레이어로 합성한다. MappingCore 는 이 포맷을 전혀
다루지 않으므로 파서와 DDS 디코더를 새로 만들어야 한다. 지형도 HD 타일 텍스처가
따로 있다.

요컨대 MappingCore 를 쓰지 않는 렌더링 경로를 하나 더 만드는 일이다. 한다면
눈에 가장 많이 띄는 유닛부터 단계적으로 옮기는 편이 낫다.

---

## SCMDraft 2 패리티

2026-09-20 기준. SCMDraft 2 0.9.10 실행 파일의 메뉴·대화상자 문자열, 공식
기능표, 한국어 메뉴 설명서를 소스와 대조한 결과다. Wine 으로 실행은 떴지만
화면 캡처는 실패해서, UI 외형은 실행 파일 문자열 기준으로 맞췄다.

**기능 목록으로 견주면 거의 다 덮었다.** 지형 세 모드, 유닛·스프라이트·두들,
가리개, 트리거 GUI+텍스트, 브리핑, 설정 창, 복사/붙여넣기, 겹쳐 보기, 브러시,
오브젝트 목록, 여러 보기 창까지 있다. 아래 "이미 있는 것"이 그 목록이다.

다만 **SCMDraft 2 실물과 화면을 나란히 놓고 견주지는 못했다.** Wine 으로
실행은 떴지만 화면 캡처가 되지 않아, 실행 파일의 메뉴·대화상자 문자열과
공식 기능표·한국어 설명서로 대조했다. 손에 익은 사람이 써 보면 쓰임새가
다른 데가 나올 수 있다.

실제 남은 일은 아래다.

### 우선 — 맵 작업에서 바로 막히는 것

#### SCMDraft 브러시 파일(.scb) 읽기

우리 브러시 팔레트는 됐다 — 담고, 이름 붙이고, 파일(.splashbrush)로 주고받고,
앱을 닫아도 남는다. 남은 것은 SCMDraft 가 만든 .scb 를 읽는 일이다.

**막고 있는 것**: .scb 는 공개 스펙이 없다. 실행 파일 역공학은 프로젝트
제약에서 금지이고, 샘플 파일도 아직 없다.

**다시 붙는다면**: 공개된 포맷 문서나 다른 오픈소스 구현을 찾거나, 샘플
파일 여러 개로 구조를 맞춰 본다. 같은 확장자로 우리 파일을 쓰지는 않는다 —
그쪽 파일인 척하게 된다.

#### MPQ 머리말을 손질해 둔 맵 열기

맵 1198 개 중 열넷이 아예 열리지 않았다. MPQ 머리말의 크기 칸이 0x20 이어야
하는데 0x10 으로 적혀 있어 StormLib 이 아카이브로 보지 않는다(오류 1004).

머리말은 한결같이 이렇게 손질되어 있다: 아카이브 크기 `0xFFFFFFFF`, 판과
조각 크기 `65535`, 해시 칸 수 상위에 `0x70`, 블록 자리 `0`.

**해 본 것** (2026-09-20):

- 머리말 크기만 `0x20` 으로 고치면 여전히 오류 1004.
- 크기·판·조각까지 고치면 **아카이브로는 열린다.** 그런데 보이는 파일이
  하나도 없다 — 해시·블록 자리를 읽지 못한다.
- 해시 자리를 16 바이트 간격으로 파일 전체에 걸쳐, 칸 수를 4~1024 로
  바꿔 가며 26624 가지를 훑었다. 시나리오를 찾지 못했다.
- 파일 안에 CHK 구역 이름(`MTXM`·`VER `·`STR ` 등)이 하나도 보이지 않는다.
  내용이 압축·암호화되어 있다는 뜻이다.

**막고 있는 것**: 해시·블록 표가 표준 자리·형태로 있지 않다. 게임
Storm.dll 의 어떤 동작에 기대는 보호로 보이며, 뚫으려면 MPQ 를 직접
파싱해야 한다. StormLib 을 쓰기로 한 제약과 어긋나고 품이 크다.

**다시 붙는다면**: 게임이 이 맵을 실제로 여는지부터 확인한다 — 열지
못한다면 고칠 까닭도 없다. 연다면 Storm.dll 이 머리말을 어떻게 읽는지가
공개 자료에 있는지 찾아본다.

### EUD — 알아 두고 넘어간 것

2026-09-20 에 EUD 를 붙이면서 실제로 재 본 것들이다. 같은 조사를 두 번 하지
않으려고 수치와 함께 적어 둔다.

**Deaths 표 배치는 유닛 바깥·플레이어 안쪽이다.** `0x0058A364` 에서
`size 48, length 228` — 한 유닛이 12플레이어 x 4바이트를 차지한다. 그전 코드는
`4 x (플레이어 x 228 + 유닛)` 으로 셈해 주소가 통째로 어긋나 있었다. EPD
나눗셈도 부호 있는 쪽이어야 한다(미네랄 `0x0057F0F0` 은 EPD −11421).

**euddraft 산출물은 `scenario.chk` 를 로케일로 숨긴다.** 중립 로케일에
0바이트짜리 미끼를 두고 영어(1033)에 진짜를 넣는다. `MpqFile::getFile` 은
**열기에 실패했을 때만** 다른 로케일을 보는데 빈 파일은 열기에 성공하므로
미끼가 잡혔다 — `readScenarioChk` 가 중립 쪽이 비면 로케일을 훑도록 고쳤다.
겪기 전에는 `splash-cli chk` 가 0바이트를 내고 round-trip 검증이 빈 바이트
끼리 견주고 있었다. 다만 사용자 컬렉션의 한국 유즈맵 400개를 훑어 보니
이 꼴을 쓰는 맵은 **0개**였다 — 지금까지의 통과율 수치에는 영향이 없다.

**euddraft macOS 배포본 0.11.0.1 은 보호(freeze)를 켜면 SIGBUS 로 죽는다.**
맵은 다 쓴 뒤라 산출물은 멀쩡하다. 크래시 지점은 `freezeMpq.so`. 업스트림
PR #178("freeze MPQ ... fix 64-bit memory bugs", 2026-09-15 머지)이 릴리스
(2026-09-09) 뒤에 고쳤으므로 다음 판에서 없어질 것으로 본다. 우리 기본은
보호 끔이라 평소에는 겪지 않는다. **다시 붙는다면**: 다음 릴리스에서 `--freeze`
로 한 번 돌려 보고, 여전히 죽으면 이 메모를 갱신한다.

**eudplib 은 페이로드 자리를 섞는다.** 같은 입력으로 세 번 돌렸더니 CHK 가
1,056,276 / 1,129,448 / 1,171,016 바이트로 매번 달랐다. 이 맵들은 CHK
바이트로 견줄 수 없다 — round-trip 규칙이 적용되지 않는 부류다.

**오프셋 표(EUD Book 의 `api.json`, 911~935개)는 라이선스 표기가 없다.**
LICENSE 도 README 도 없고, 담긴 설명문이 EUDDB 계열 문구("The official name
for this is g_ActivePlayerID")라 원작자가 armoha 가 아닐 수 있다 — 물어도 줄
권한이 없을 수 있다는 뜻이다. 그래서 저장소에 넣지 않고 `--db` /
'오프셋 표 불러오기' 로 가리키게 했다.

**대신 eudplib(MIT)에서 뽑았다.** `src/eudplib/scdata` 에 이름 붙은 절대 주소가
151개 있다(DAT 표 여덟 가지 + 플레이어 상태). 붙박이 표가 24개에서 165개로
늘었다. 뽑는 일은 `tools/gen_eud_offsets.py` 가 한다.

뽑은 표를 EUD Book 과 대조해 보았다: **크기가 어긋난 것 0개**, 크기·개수가
모두 같은 것 141개, 개수만 다른 것 7개(6개는 우리가 길게, 1개는 짧게),
Book 에 아예 없는 것 3개. 개수는 다음 자리까지의 틈으로 조이는데, 틈이 넓은
자리는 실제보다 길게 잡힐 수 있다 — 이름이 조금 넘겨 붙을 뿐 값에는 영향이
없다.

**남는 것**: 리마스터 지원 여부(`scr`)는 eudplib 에 없다. `eud check` 의
판정은 붙박이 24개에 대해서만 촘촘하고, 나머지는 EUD Book 을 얹어야 한다.

**아직 안 한 것**:

- epScript 자동 완성. EUD Editor 3 이 동봉하는 `epsFunctions.txt` 는 함수
  4,839줄을 한/영 설명과 함께 담고 있다(MIT). 형식을 읽어 붙이면 된다.
  지금은 구문 강조만 있다.
- `MemoryEPD` 표기(주소 대신 EPD 로 직접 적기). MappingCore 의 텍스트
  트리거는 EPD 를 그대로 쓰므로 표기만 고르면 된다.
- 조건·동작을 낱개로 더하는 일반 명령 — 지금은 EUD 만 통째로 넣는다.
- EUD Editor 3 의 EUD 아닌 부분 — DAT 편집기(유닛 능력치를 직접), Firegraft
  버튼셋, iscript, SCArchive 올리기. EUD 가 아니라 별개 갈래라 손대지 않았다.
- euddraft 데몬 모드(`.edd` 로 파일을 지켜보며 다시 굽기). 프로세스가 끝나지
  않아 다루기 까다롭다. 우리는 `.eds` 한 번 실행만 쓴다.

**하지 않기로 한 것**: epTrace 급 인게임 디버거. 리마스터에 메모리 스캐너를
붙이는 일이라 계정이 위험하다.

### CLI 에서 아직 안 되는 것

2026-09-20 에 갈래별 명령(`splash-cli unit place ...`)을 붙이면서 "화면에서
되는 편집이 명령으로는 안 된다"는 항목은 닫았다. 코어에 있는데 CLI 에만
없던 것도 다 이었다 — 조건·동작 낱개 편집, 브리핑 실행 플레이어, 모드 자료
얹기(`--mod`), AI 타운 읽기, 두들 켜고 끄기까지 된다.

남은 것은 하나다.

- **브러시 팔레트(.splashbrush) 를 명령으로 찍기.** 코어에 없다 — 읽고 쓰는
  코드가 `src/ui/brush_palette.cpp` 에만 있고, 형식이 QDataStream(Qt_6_0,
  리틀엔디언)이라 코어로 올리려면 splash_io 에 Qt 를 링크해야 한다.
  `map_archive.h` 머리말이 "Qt 타입은 코어 전체에서 금지" 라고 못박은 경계다.

  **옮기지 않기로 했다.** `.splashbrush` 는 이름 붙인 브러시를 여럿 모아 두는
  **팔레트**(앱을 닫아도 남는 서랍)이고, CLI 의 `splash-tiles`·`splash-objects`
  는 조각 하나를 주고받는 **클립보드**다. 목적이 달라 하나로 묶으면 양쪽 다
  어정쩡해진다. 다리가 필요하면 형식을 합치지 말고 GUI 에 "브러시를
  splash-tiles 로 내보내기" 를 다는 편이 싸다.

  형식은 이렇다 (다시 조사하지 않도록): 매직 `"SPLB"`, u16 버전(2 부터
  마스크가 붙는다), u32 개수, 그다음 브러시마다 이름(QString 직렬화,
  UTF-16 길이 접두)·가로·세로·타일셋·타일 수·타일들·마스크 길이·마스크.
  모두 리틀엔디언.

**인코딩은 맵에 저장되지 않는다.** CHK 에 코드 페이지 칸이 없다. 그래서
`map encoding` 은 보여 주기만 하고, 실제로 고칠 때는 글자를 쓰는 명령에
`--encoding cp949` 를 함께 줘야 한다. 한 번 "바꿔 저장" 하는 명령으로
만들었다가 CHK 가 한 바이트도 안 달라지는 것을 보고 되돌렸다.

**주의할 것**: 명령을 더할 때마다 실제 맵으로 열기 → 고치기 → 저장 →
다시 열기를 돌려 확인한다. 지형은 칠했다가 되돌리면 CHK 가 한 바이트도
달라지지 않아야 한다 — 그것이 가장 빠른 회귀 검사다.

### 나중 — 편집기 인프라

맵 내용보다 편집기 껍데기. 패리티 목록에는 들어가지만 당장 맵이 안 만들어지지는
않는다.

- **플러그인** (TrigEdit.sdp, TrigEditPlus). SCMDraft 전용 ABI. 다시 만들 가치는
  낮고, 텍스트 트리거·EUD 를 에디터 안에 두는 편이 맞다.
- **그래픽 뷰어 · Iscript 디버그** — Debug 메뉴. 그림 버퍼 비우기는 있다
  (보기 메뉴). 유닛·타일 그림은 팔레트에서 이미 훑어볼 수 있어 뷰어를
  따로 둘 값어치는 낮다.
- **업데이트 확인 · 파일 연결 · 시작 메뉴** — Windows 설치 유틸.

### 이미 있는 것 (다시 넣지 말 것)

대조할 때 빠진 줄 알고 되풀이하지 않도록. 새 맵/열기/저장/최근 파일/여러 맵
탭, 줌, Isometric·Rectangular·Subtile, 타일 팔레트, 두들, 유닛 1–12·겹치기·
스냅, 스프라이트 놓기, 로케이션 목록·이동, MASK 칠하기, 크립 표시, 애드온·
나이더스 연결, 맵 속성(이름·설명·크기·타일셋), 플레이어 종족·슬롯·세력 이름·
색, 유닛/업그레이드/기술 설정, 문자열, 소리, 트리거 GUI+텍스트, 브리핑, 스위치,
CUWP, 실행 취소, 유닛 복사/붙여넣기/잘라내기, 지형 좌우·상하·4방 대칭,
배치 소리, 미니맵, 보호 해제, 유닛 228종, 지형 복사/잘라내기/붙여넣기(브러시로
찍기), 브러시 팔레트(.splashbrush 저장·불러오기), 맵에서 로케이션 그리기·
모서리 조절·안팎 뒤집기, 세력 동맹·공유 시야·동맹 승리·시작 위치 섞기,
끌어서 유닛 여럿 고르기(Shift 로 더하기), 우클릭 메뉴, 지형 여러 덩어리
고르기(Shift), 로케이션 복사/붙여넣기, 지형 겹쳐 보기(높이·지날 수 있는 곳·
지을 수 있는 곳·크립), 타일 번호 표시, 유닛 사거리·시야·탐지 원, 파일런
전력 범위, 안개 미리보기, 길 영역 나눠 보기, 크립 옅게, 맵 리빌러 감추기,
AI 타운 표시, 맵 그림으로 저장, 리빌러 깔기·지우기, 가리개 일괄·복사/붙여넣기,
자원량 섞기, 모두 고르기, 시작 위치로 가기, 유닛 겹쳐 쌓기, 스프라이트 고르기·
속성, 격자 크기·색, 회전 대칭, 두들을 지형으로 풀기, 옵저버 트리거,
어긋난 두들 찾기·고치기, 두들 고르기·복사/붙여넣기/잘라내기, 자동 저장,
오브젝트 목록(유닛·스프라이트·두들·로케이션),
EUD(Memory·Memory Masked 조건·동작을 주소로 편집, 주소 계산기와 오프셋 표 찾기,
맵 안 EUD 훑기와 리마스터 지원 여부 가려내기, euddraft 로 epScript 빌드),
같은 맵 여러 보기 창·늘어놓기, 모드 자료(MPQ) 얹기·프로필, 그림 버퍼 비우기,
스프라이트 복사/붙여넣기, 유닛 팔레트 이름·번호 찾기,
지형에 따라 달라지는 플레이어 색(얼음·사막), 꼬리를 겹쳐 담는 문자열 저장,
배율 12.5~800%, 유닛·스프라이트 옅게 보기, 지형 베낄 때 두들 빼기,
맵 밖으로 나간 것 치우기(열 때 자동으로도).

CLI 쪽으로는: 갈래별 명령(unit·sprite·doodad·location·terrain·fog·map·player·force·string·switch·preset·unitdef·upgrade·tech·scenario·sound·trigger·briefing·eud), 유닛을 이름으로 고르기, 지형·가리개를 파일로 오려 붙이기, 지형 대칭, 트리거 더하기·지우기·베끼기·실행 플레이어, EUD 주소 셈·오프셋 표·맵 훑기·조건과 동작 넣기·euddraft 빌드, 저장 전 원본 지키기(--in-place 는 바꿔치기), 저장본 다시 열어 확인, 유닛·스프라이트·두들·로케이션을 네모째 오려 붙이기(`object copy/paste`, 애드온·나이더스 연결과 로케이션 높이·안팎 뒤집힘까지 따라온다).
