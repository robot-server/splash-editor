# 백로그

지금 당장 급하지 않지만 잊으면 안 되는 것들. 각 항목에 "무엇이 막고 있는지"를
적어 두었다 — 나중에 다시 붙을 때 조사부터 다시 하지 않기 위해서다.

## 그래픽

### 크립 가장자리가 게임과 다르다

건물마다 타원으로 범위를 잡고 테두리만 부드럽게 처리한다. 게임 화면과 나란히
놓으면 경계 모양이 다르다.

**막고 있는 것**: 게임의 크립 확산 패턴(건물별로 어느 타일까지 퍼지는지)이
데이터 파일이 아니라 실행 파일 안에 있다. 타일셋에 가장자리 전용 타일이 없다는
것은 확인했다 (README 의 "크립 가장자리에 대하여" 참고).

**다시 붙는다면**: 게임을 실제로 띄워 크립 모양을 관찰하고 타일 패턴을 표로
만드는 방법이 남아 있다. 실행 파일 역공학은 프로젝트 제약에서 금지.

### 유닛 애니메이션이 없다

정지 자세만 그린다. `AnimContext::animate(tick)` 에 타이머를 물리면 되지만,
에디터에서 유닛이 움직이면 좌표를 잡기 불편하다. SCMDraft 2 도 정지 화면이다.

### 유닛 특수 상태가 반영되지 않는다

리프트된 건물, 애드온 연결, 버로우 상태 등이 기본 자세로 그려진다.
`Chk::Unit::stateFlags` 와 `relationFlags` 를 액터 초기화에 반영해야 한다.

## 문자열

### 한국어 맵 이름이 깨져 보인다

CHK 문자열을 UTF-8 로 취급한다(MappingCore 의 전제). 한국어 맵은 대개 CP949 라
이름·설명이 깨진다. round-trip 바이트에는 영향이 없다 — 원본 바이트는 그대로
보존한다. 표시 계층에서만 인코딩을 추정해 변환하면 된다.

**주의**: 추정이 틀리면 멀쩡한 UTF-8 맵을 깨뜨릴 수 있다. UTF-8 로 디코딩해
보고 실패할 때만 CP949 로 재시도하는 순서가 안전하다.

## 맵 호환

### 보호된 맵은 저장이 거부된다

규격을 벗어난 맵(문자열 65536개 선언, VER 섹션 누락 등)은 MappingCore 가 쓰기를
거부한다. 데이터를 조용히 망가뜨리는 것보다 나은 동작이라 그대로 두었다.
편집 대상으로 삼으려면 어느 선까지 복구할지 정책이 필요하다.
